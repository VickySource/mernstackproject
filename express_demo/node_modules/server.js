const express = require('express');
const mongoose = require('mongoose');

const app = express();
const PORT = 3000;


// the originAgentClusterl url that was copied is mongodb+srv://sampreethchsampreethch_db_user:vhL8XynsFcfTkRaW@sampreeth.9iyksjm.mongodb.net/ add the database  at the end like test.db
const mongoURI = "mongodb+srv://Vicky:1nyLydIRu3YC8EBJ@cluster0.qrdfhb9.mongodb.net/test.db"
mongoose.connect(mongoURI).then(()=>{
    console.log("Connected to MongoDB");
})
.catch((err)=>{
    console.error("Connection to mongoDB shows an error");
})

const newSchema = new mongoose.newSchema({
    name:{
        type: String,
        required: true
    },
    quantity: Number,
    date: {
        type: Date,
        default: Date.now
    }
})

app.post()